# Esperado

- Seleccionar `database_reviewer`, `security_reviewer` y `test_reviewer`.
- Base de datos cubre corrección y parametrización; seguridad cubre
  explotabilidad e impacto.
- Consolidar una sola causa raíz si ambos describen la misma consulta.
- Un hallazgo `Important` o `Critical` exige `BLOCKED`.
