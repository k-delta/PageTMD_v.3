# Esperado

- Reutilizar la revisión general del mismo rango.
- Seleccionar `security_reviewer` y `test_reviewer`.
- No seleccionar arquitectura por tratarse de un cambio rutinario de un archivo.
- Consolidar el defecto de autorización como `Important` o `Critical` según el
  impacto demostrado y emitir `BLOCKED`.
